# Postgres the app reads through the Prisma pg driver adapter.
# On Railway this is ${{Postgres.DATABASE_URL}} over the private network;
# locally it is the same database through its public TCP proxy.
DATABASE_URL="postgresql://user:password@host:5432/railway"
